{% include 'default/_form.html.twig' with {
    page_title: 'title.product_accept',
    page_title_param: {'%id%': product.id},
    attr: {
        method: 'PUT',
        action: url('product_accept', {'id': product.id}),
    },
    back_to_list_path: 'product_index',
} %}