{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.diary_entry_new'|trans }}
{% endblock %}

{% block body %}
    <h1>{{ 'title.diary_entry_new'|trans }}</h1>
    {{ form_start(form, { method: 'POST', action: url('diary_entry_new') }) }}
    {{ form_widget(form) }}
    <div class="form-group row float-sm-right">
        <input type="submit" value="{{ 'action.save'|trans }}" class="btn btn-primary" />
    </div>
    <div class="form-group row float-sm-left">
        <a href="{{ url('diary_entry_index') }}" class="btn btn-link">
            {{ 'action.back_to_list'|trans }}
        </a>
    </div>
    {{ form_end(form) }}
{% endblock %}